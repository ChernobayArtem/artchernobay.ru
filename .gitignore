.DS_Store
# исходники-скриншоты грузятся как PNG в cases/<кейс>/ и удаляются после сжатия в JPG —
# на случай если PNG останется, не даём ему попасть в коммит (в деплой идут только JPG)
cases/**/*.png
